
#include <iostream>
using namespace std;

int main()
{
    int rev=3000, sec=60, degree=360, product, x;
      
        //Calculation
    product = rev * degree;
    x = product / sec;

    cout << "Problem: An Ball is spinning and creates 3000 revolutions per minutes. calculate the degrees it rotates in one second." << endl;
        cout << "\nFormula:\nP=rev*degreee\nx=P/sec" << endl;
    cout << "\n3000*360=1,080,000\nx=1,080,000/60\nx=18000." << endl;

    //Calculation
    product = rev * degree;
    x = product / sec;

    cout << "\nThe total degrees of the ball it spins in a second is " << x << "°";

    return 0;
}
